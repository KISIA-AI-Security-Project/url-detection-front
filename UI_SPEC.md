# UI_SPEC.md

## 1. 문서 목적

본 문서는 Chrome Extension 기반 URL 분석 서비스의 UI/UX 구현 명세를 정의한다.

본 서비스는 다음 두 화면을 중심으로 구성한다.

1. **Chrome Extension Popup**
   - 현재 탭 URL 자동 입력
   - 사용자가 직접 URL 수정 가능
   - 고속 분석 실행
   - `스캔 중...` 로딩 상태 표시
   - 고속 분석 결과 표시
   - 심층 분석 진행 여부 선택

2. **상세 분석 페이지**
   - 심층 분석 진행 상태 표시
   - 분석 취소
   - 최종 판정 표시
   - 핵심 근거 및 L2/L3/L4 상세 근거 제공

시각 디자인은 별도 `DESIGN.md`를 우선 적용한다.

---

# 2. UI 설계 원칙

## 2.1 우선순위

UI 구현 시 다음 우선순위를 따른다.

1. `DESIGN.md`의 시각 디자인 규칙
2. 본 문서(`UI_SPEC.md`)의 화면 구조 및 UX 흐름
3. 구현 편의를 위한 임의 스타일

색상, 폰트, spacing, radius, shadow, button style 등은 `DESIGN.md`를 우선한다.

화면 구성, 상태 전이, 버튼 동작, 오류 처리 등은 본 문서를 우선한다.

---

## 2.2 전체 디자인 방향

전체 UI는 다음 방향을 따른다.

- 밝고 미니멀한 Vercel / Linear / Notion 기반 스타일 (`DESIGN.md` 기준)
- 불필요한 장식 최소화
- 넉넉한 여백
- 얇은 border / divider
- 카드 남발 금지
- 핵심 정보 위주로 정보 위계 구성
- 위험 상태만 색상으로 강조
- 과도한 보안 대시보드 스타일 지양
- 기술 정보는 본문보다 낮은 시각적 우선순위로 표현

다크 모드는 초기 버전에서 지원하지 않는다.

---

# 3. 화면 구성

서비스는 다음 두 화면으로 구성한다.

```text
Chrome Extension Popup
        │
        ├─ URL 입력
        ├─ 고속 분석
        ├─ 진행 상태
        ├─ 고속 분석 결과
        └─ 심층 분석 진입
                │
                ▼
        /analysis/{job_id}
                │
                ├─ 스캔 중
                ├─ 분석 취소
                └─ 최종 분석 결과
```

---

# 4. Chrome Extension Popup

## 4.1 기본 레이아웃

팝업은 **세로형 단일 컬럼 구조**를 사용한다.

권장 크기:

- width: 약 `340px`
- min-height: 약 `200px`
- max-height: 약 `540px`
- 최대 높이 초과 시 내부 스크롤

상단 브랜드 영역은 최소화한다.

```text
[작은 로고] 서비스명

분석할 URL
[ URL 입력 ]

[ 분석 시작 ]

[상태 / 결과 영역]
```

파비콘은 표시하지 않는다.

---

## 4.2 URL 입력

### 기본 동작

팝업을 열면 현재 활성 탭의 URL을 자동으로 입력한다.

사용자는 입력된 URL을 직접 수정할 수 있다.

### 지원 프로토콜

지원:

- `http://`
- `https://`

지원하지 않음:

- `chrome://`
- `chrome-extension://`
- `file://`
- `about:`
- 기타 비웹 프로토콜

현재 탭 URL이 분석 불가능한 경우:

- URL 입력창은 빈 상태
- `분석 시작` 버튼 비활성
- 사용자가 유효한 URL을 입력하면 버튼 활성화

### 프로토콜 자동 보완

사용자가 프로토콜 없이 입력한 경우:

```text
example.com
```

다음과 같이 자동 보완한다.

```text
https://example.com
```

사용자가 `http://`를 직접 입력한 경우 변경하지 않는다.

### URL 정규화

분석 시작 전 URL을 정규화하고, 정규화된 값을 입력창에 다시 표시한다.

예:

```text
HTTPS://Example.COM/login/
```

→

```text
https://example.com/login/
```

단, 다음 요소는 유지한다.

- path
- query
- fragment

예:

```text
https://example.com/login?redirect=/account#verify
```

전체 URL을 분석 대상으로 유지한다.

---

## 4.3 URL Validation

입력 중에는 다음만 실시간 처리한다.

- URL 유효성 판단
- `분석 시작` 버튼 활성 / 비활성

오류 메시지는 입력 중 지속적으로 표시하지 않는다.

다음 상황에서만 오류 문구를 표시한다.

- 명백히 잘못된 URL
- 사용자가 `분석 시작`을 클릭했으나 유효하지 않은 경우

예:

```text
올바른 URL 형식이 아닙니다.
```

또는:

```text
이 URL 형식은 분석할 수 없습니다.
```

---

# 5. 고속 분석 시작

## 5.1 버튼

버튼 문구:

```text
분석 시작
```

역할:

- Primary Button

---

## 5.2 분석 시작 후 상태

분석이 시작되면:

- URL 입력창 비활성
- `분석 시작` 버튼 비활성
- 다른 URL 입력 불가
- 새 고속 분석 시작 불가

고속 분석은 동시에 **1건만 실행**한다.

---

# 6. 고속 분석 진행 상태

심층 분석과 동일하게 작은 Loading Indicator와 다음 문구만 표시한다.

```text
스캔 중...
```

단계 목록, 단계별 완료 / 진행 / 대기 상태, 진행률 `%`는 표시하지 않는다.

---

## 6.1 팝업 종료 / 재오픈

고속 분석 중 사용자가 팝업을 닫아도 분석은 계속 진행한다.

팝업을 다시 열면 현재 작업 상태를 복구한다.

복구 상태:

- 진행 중이면 `스캔 중...` 로딩 상태 표시
- 완료되었으면 결과 표시

---

# 7. 현재 탭 URL 변경 처리

분석 상태에 따라 현재 탭 URL 자동 반영 규칙을 다르게 적용한다.

### 분석 중이 아닐 때

현재 탭 URL 변경 시 입력창을 자동 갱신한다.

### 분석 중일 때

기존 분석 대상 URL을 유지한다.

### 결과 확인 중일 때

기존 분석 결과의 URL을 유지한다.

### `종료` 이후

초기 화면으로 복귀하면서 현재 탭 URL을 다시 반영한다.

---

# 8. 고속 분석 결과

## 8.1 판정 체계

사용자에게 표시하는 판정은 3단계로 제한한다.

- 안전
- 주의
- 위험

내부 점수나 confidence 값은 사용자에게 노출하지 않는다.

---

## 8.2 결과 표시 순서

팝업 결과는 다음 순서를 따른다.

```text
[판정]

[한줄 설명]

[핵심 근거]

[심층 분석 안내]

[종료] [심층 분석]
```

예:

```text
주의

주의가 필요한 신호가 확인되었습니다.

핵심 근거
• 비정상 페이지 이동
• 외부 도메인 로그인 전송
• 인증서 이상

더 자세한 분석을 진행할까요?

[ 종료 ]   [ 심층 분석 ]
```

---

## 8.3 한줄 설명

고속 분석의 한줄 설명은 **LLM을 사용하지 않는다.**

판정별 결정론적 템플릿으로 생성한다. `주의` 판정의 팝업 설명에는 개별 탐지 내용을 나열하지 않고 간단한 주의 문구만 표시한다. 구체적인 내용은 바로 아래 핵심 근거에서 제공한다.

```text
주의
```

예:

```text
주의
```

→

```text
주의가 필요한 신호가 확인되었습니다.
```

---

# 9. 핵심 근거

## 9.1 팝업

팝업에서는 최대 **3개**만 표시한다.

별도 카드 여러 개로 분리하지 않고 하나의 영역 / 카드 안에서 리스트로 표시한다.

### 정렬 기준

다음 우선순위를 사용한다.

1. severity
2. confidence
3. 탐지 순서

`confidence`는 내부 정렬용이며 사용자에게 표시하지 않는다.

---

# 10. 결과 상태별 UI

상태 색상은 다음 의미를 사용한다.

- 안전 → Green 계열 (`DESIGN.md`의 safe 토큰)
- 주의 → Amber / Yellow 계열
- 위험 → Red 계열

단, 정확한 색상값은 `DESIGN.md`를 따른다.

상태 색은 다음 요소에만 강하게 적용한다.

- 상태 아이콘
- 판정 텍스트
- accent

배경은 매우 옅은 상태색만 사용할 수 있다.

화면 전체를 상태색으로 채우지 않는다.

---

# 11. 상태 아이콘

상태는 색상만으로 구분하지 않는다.

항상 `아이콘 + 텍스트`를 함께 사용한다.

예:

- 안전 → Check 계열
- 주의 → Warning 계열
- 위험 → Danger / Block 계열
- 분석 중 → Loading Indicator

아이콘 스타일은 `DESIGN.md`를 우선한다.

---

# 12. 팝업 CTA

고속 분석 결과에서는 항상 다음 두 버튼을 제공한다.

```text
[ 종료 ]   [ 심층 분석 ]
```

### 종료

- Secondary Button
- 현재 결과 초기화
- 팝업은 닫지 않음
- 초기 URL 입력 화면으로 복귀
- 현재 활성 탭 URL 재반영

### 심층 분석

- Primary Button
- 새 탭에서 상세 분석 페이지 오픈
- Chrome Popup은 포커스를 잃으며 자연스럽게 닫힘

---

# 13. 심층 분석 제공 정책

고속 분석 판정과 관계없이 항상 심층 분석 기능을 제공한다.

단, 버튼 강조 수준은 판정에 따라 달라질 수 있다.

- 안전 → 심층 분석은 보조적 성격
- 주의 → 심층 분석 강조
- 위험 → 심층 분석 가장 강하게 강조

기능 자체는 항상 제공한다.

---

# 14. 상세 분석 페이지

## 14.1 URL

상세 분석 페이지 URL:

```text
/analysis/{job_id}
```

실제 분석 대상 URL은 주소창에 직접 포함하지 않는다.

---

## 14.2 상태 복구

상세 페이지는 `job_id`를 기준으로 상태를 복구한다.

따라서 사용자가:

- 새로고침
- 페이지 종료 후 재접속

해도 동일한 분석 상태를 조회할 수 있어야 한다.

---

# 15. 상세 분석 진행 화면

심층 분석의 내부 단계를 사용자에게 노출하지 않는다.

다음 정도만 표시한다.

```text
스캔 중...

페이지를 닫거나 새로고침해도
분석 상태는 유지됩니다.

[분석 취소]
```

구성:

- 작은 Loading Indicator
- `스캔 중...`
- 상태 유지 안내 문구
- `분석 취소`

퍼센트 진행률 없음.

세부 단계 없음.

---

# 16. 심층 분석 취소

사용자는 심층 분석 진행 중 취소할 수 있다.

버튼:

```text
분석 취소
```

취소 시 해당 `job_id`는 cancelled 상태로 유지한다.

취소 화면:

```text
분석이 취소되었습니다.

요청한 심층 분석이 중단되었습니다.

[닫기]
```

재분석 버튼은 제공하지 않는다.

---

# 17. 심층 분석 완료

심층 분석 완료 시 사용자가 새로고침할 필요가 없어야 한다.

상태를 자동 확인하고:

```text
스캔 중...
```

→

```text
완료
```

→

```text
최종 결과
```

순서로 자동 전환한다.

`완료` 상태는 매우 짧게 표시하고 바로 결과 화면으로 전환한다.

결과 표시 시 페이지를 자동으로 최상단 `최종 판정` 영역으로 이동한다.

---

# 18. 서버 연결 오류

상세 분석 페이지에서 서버 연결이 일시적으로 끊긴 경우 즉시 실패 처리하지 않는다.

다음 상태를 표시한다.

```text
연결을 다시 확인하는 중입니다...
```

자동 재시도 후 정상 연결되면:

- 분석 중 화면
- 또는 완료 결과 화면

으로 자동 복구한다.

일정 횟수 이상 실패한 경우에만 오류 상태를 보여준다.

```text
상태를 불러오지 못했습니다.

[다시 시도]
```

---

# 19. 심층 분석 실패

실제 분석 작업이 실패한 경우:

```text
심층 분석을 완료하지 못했습니다.

분석 과정에서 오류가 발생했습니다.
잠시 후 다시 시도해주세요.

[닫기]
```

상세 분석 페이지에서는 재분석 기능을 제공하지 않는다.

---

# 20. 최종 결과 페이지 구조

최종 결과 페이지는 **한 페이지 섹션형**으로 구성한다.

탭 기반 UI를 사용하지 않는다.

전체 구조:

```text
[헤더]

[분석 URL / 완료 시각]

[최종 판정]

[초기 분석 결과]

[핵심 근거]

[네트워크 분석 L2]

[콘텐츠 분석 L3]

[클로킹 / 행동 분석 L4]
```

---

# 21. 헤더

상세 페이지 헤더는 최소 구성으로 한다.

좌측:

```text
[작은 로고] 서비스명
```

우측:

- 별도 메뉴 없음
- 별도 네비게이션 없음

---

# 22. 상세 페이지 본문 폭

본문은 중앙 정렬된 고정 최대폭 구조를 사용한다.

권장:

```text
max-width: 960px
```

화면이 넓어도 내용이 과도하게 퍼지지 않도록 한다.

---

# 23. 분석 메타 정보

최종 판정 카드의 분석 정보 영역에 다음 정보를 표시한다.

- 분석 URL
- 분석 완료 시각
- 소요 시간

예:

```text
example.com
분석 완료 · 2026.09.08 13:42
```

URL은 클릭 가능한 링크로 만들지 않는다.

---

# 24. 최종 판정

심층 분석 결과를 `최종 판정`으로 가장 크게 표시한다.

고속 분석 결과는 `초기 분석 결과`로 작게 남긴다.

예:

```text
최종 판정
위험

피싱 사이트로 의심되는 여러 행동이 확인되었습니다.

초기 분석 결과
주의
```

---

# 25. 초기 판정과 최종 판정 변화

초기 판정과 최종 판정이 다른 경우에만 변화를 강조한다.

예:

```text
초기 분석  주의
      ↓
최종 판정  위험
```

판정이 동일하면 변화 표시를 생략한다.

---

# 26. 최종 판정 카드

최종 판정은 카드 형태로 강조한다.

단:

- 카드 전체를 강한 상태색으로 채우지 않는다.
- 기본 배경은 `DESIGN.md` 스타일 유지
- 상태 아이콘
- 판정 텍스트
- accent

에만 상태색을 강조한다.

---

# 27. 최종 판정 설명

최종 판정 자체는 규칙 / 분석 로직이 결정한다.

LLM은 판정을 결정하지 않는다.

LLM의 역할:

```text
L2 + L3 + L4 근거
→ 사용자용 최종 설명 1~2문장 생성
```

예:

```text
로그인 정보 입력을 유도한 뒤 외부 도메인으로 전송하는 행동과
다단계 리다이렉션이 확인되어 피싱 위험이 높은 것으로 판단됩니다.
```

LLM 실패 시 전체 결과를 실패 처리하지 않는다.

Fallback 예:

```text
위험 신호가 여러 개 확인되었습니다.
아래 상세 근거를 확인해주세요.
```

---

# 28. 상세 근거 설명

개별 상세 근거 설명은 LLM을 사용하지 않는다.

모든 상세 근거 설명은 템플릿 기반으로 생성한다.

각 항목 구조:

```text
사용자용 제목
기술 라벨

설명 1~2줄
```

예:

```text
외부 도메인으로 로그인 정보 전송
Form Action Domain

로그인 폼의 전송 대상이 현재 사이트와 다른 도메인으로 확인되었습니다.
```

---

# 29. 핵심 근거 — 상세 페이지

상세 페이지에서는 핵심 근거를 최대 **5개**까지 표시한다.

핵심 근거는 항상 펼쳐진 상태로 유지한다.

핵심 근거와 아래 L2/L3/L4 상세 근거의 중복은 허용한다.

역할:

```text
핵심 근거 = 요약
상세 섹션 = 상세 검증
```

---

# 30. 핵심 근거 클릭 동작

핵심 근거를 클릭하면 해당 상세 근거 위치로 이동한다.

동작:

1. 관련 L2/L3/L4 섹션 자동 펼침
2. 해당 근거 위치로 스크롤 이동

---

# 31. L2 / L3 / L4 섹션

상세 결과는 다음 섹션을 유지한다.

```text
네트워크 분석
L2

콘텐츠 분석
L3

클로킹 / 행동 분석
L4
```

각 섹션은 아코디언 방식으로 접고 펼칠 수 있다.

---

# 32. 섹션 탐지 건수

각 섹션 제목에 의미 있는 탐지 근거 개수를 작은 숫자 배지로 표시한다.

예:

```text
네트워크 분석        3
콘텐츠 분석          2
클로킹 / 행동 분석   1
```

`3건 탐지` 같은 강한 문구보다 작은 숫자 badge를 사용한다.

---

# 33. 탐지 0건 섹션

탐지 근거가 0건이어도 섹션을 숨기지 않는다.

펼쳤을 때:

```text
특이사항이 확인되지 않았습니다.
```

로 표시한다.

이는 해당 분석 영역이 실제로 수행되었음을 사용자에게 보여주기 위함이다.

---

# 34. 기본 펼침 규칙

L2/L3/L4는 기본적으로 접힌 상태로 시작한다.

단, **탐지 건수가 가장 많은 섹션 하나만 자동으로 펼친다.**

동일한 탐지 건수인 경우 우선순위:

```text
L4 → L3 → L2
```

---

# 35. 상세 근거 내부 레이아웃

각 L2/L3/L4 레이어와 상세 근거는 흰색의 얇은 테두리 박스로 구분한다. 탐지 근거가 없어도 세 레이어 박스를 모두 표시한다. L2/L3/L4 콘텐츠의 들여쓰기를 제거하고 전체 폭을 사용한다. 접힌 근거 박스에는 제목과 기술 라벨만 표시하고, 설명과 원시값은 함께 펼쳐 같은 왼쪽 기준선에 정렬한다.

Table 형태보다 **세로형 리스트**를 우선한다.

예:

```text
비정상 페이지 이동
Redirect Chain

총 3회의 리다이렉션이 발생했습니다.
```

---

# 36. 의미 있는 결과 중심 표시

상세 페이지에서도 모든 정상 항목을 나열하지 않는다.

기본 표시 대상:

- 탐지된 항목
- 위험 / 주의 판단에 의미가 있는 항목
- 사용자 이해에 필요한 보조 정보

다음과 같은 항목을 반복 나열하지 않는다.

```text
강제 다운로드 없음
HTTP Refresh 없음
IP Redirect 없음
Content-Type 불일치 없음
...
```

필요한 경우 섹션 수준에서 다음 정도의 요약은 허용한다.

```text
검사된 항목 8개 · 이상 2개
```

---

# 37. 상세 근거 펼침

근거 설명과 Raw Data는 기본 화면에서 숨긴다.

별도 `상세 보기` 버튼을 두지 않는다. 사용자가 각 근거 박스의 헤더를 누르면 설명과 Raw Data를 함께 표시하며, 같은 동작으로 다시 접을 수 있어야 한다.

예:

```text
외부 도메인으로 로그인 정보 전송
Form Action Domain
[펼침 아이콘]
```

펼친 상태:

```text
로그인 폼의 전송 대상이 현재 사이트와 다른 도메인으로 확인되었습니다.

Target URL
https://login-example.net/auth

Current Domain
example.com

Target Domain
login-example.net
```

---

# 38. 원시값 표시 형식

JSON을 그대로 노출하지 않는다.

기본 형식:

```text
Key
Value
```

즉, **Key-Value UI**를 사용한다.

다음과 같은 기술 값에는 monospace 폰트를 적용한다.

- URL
- Domain
- IP
- Hash
- Certificate Fingerprint
- 기타 식별자

---

# 39. 긴 기술 값

긴 URL / Hash / Fingerprint는 기본적으로 말줄임 처리한다.

예:

```text
https://very-long-example-domain.com/path/to/...
```

사용자가 `전체 보기`를 선택하면 전체 값을 표시한다.

---

# 40. URL 링크 정책

분석 결과에 포함된 URL / Domain은 클릭 가능한 링크로 만들지 않는다.

목적:

- 위험 사이트로 오클릭 방지
- 보안 분석 결과 화면의 안전성 유지

값은 텍스트로만 표시한다.

---

# 41. 복사 기능

다음과 같은 기술 값에는 작은 복사 버튼을 제공할 수 있다.

- URL
- Domain
- IP
- Hash
- Fingerprint

복사 완료 시 별도 Toast를 띄우지 않는다.

인라인으로 잠시:

```text
복사됨
```

상태를 표시한다.

---

# 42. 카드 사용 규칙

대부분의 설명 텍스트는 배경과 구분되는 박스 안에서 관리한다. 박스는 동일한 흰색 surface, 얇은 border, 8px radius를 사용해 과도하게 강조되지 않도록 한다.

카드 사용:

- 분석 URL / 완료 시각
- 최종 판정
- 핵심 근거
- L2/L3/L4 카테고리
- 개별 상세 근거 (들여쓰기 없는 테두리 박스)
- 결과 하단 안내 문구

일반 섹션 사용:

- 진행 상태
- 기타 메타 정보
- `분석 결과`, `핵심 근거`, `상세 분석` 등의 페이지·주요 섹션 제목

페이지 헤더, 푸터, 페이지 제목, 주요 섹션 제목은 화면 구조를 나타내므로 박스 밖에 유지한다.

카드 남발을 피한다.

---

# 43. 섹션 구분

섹션 간 구분은 다음을 우선한다.

- 여백
- 얇은 divider

강한 border box를 반복 사용하지 않는다.

---

# 44. Animation / Motion

애니메이션은 기능 이해를 돕는 용도를 우선하며, 판정과 근거의 정보 위계를 보여주는 짧은 진입 효과도 제한적으로 사용할 수 있다.

### 고속 분석 완료 전환

`스캔 중...` 상태가 완료되면 결과 영역 전체를 `220ms ease-out`으로 표시한다.

- opacity: `0 → 1`
- translateY: `4px → 0`
- 최초 결과 표시 시 한 번만 실행
- 판정 아이콘에는 최대 3%의 1회 scale 효과를 적용할 수 있음
- 판정, 핵심 근거, 보조 안내, 액션 블록은 DOM 순서에 따라 `50ms` 간격으로 순차 등장
- 각 블록의 이동 거리는 8px 이하로 제한
- 애니메이션 완료를 기다리지 않고 완료 상태를 보조 기술에 즉시 알림

### 허용

- 분석 중 Loading Indicator
- `스캔 중...` 아이콘과 문구의 `1.8s` 낮은 대비 pulse
- Accordion open / close
- Button hover / press
- 고속 분석 완료 결과의 fade-in + 4px 상향 이동
- 상세 결과 페이지의 제목, 최종 판정, 핵심 근거, 상세 분석, 안내 문구, 푸터가 DOM 순서에 따라 `50ms` 간격으로 등장하는 짧은 stagger fade-in
- L2/L3/L4 레이어 박스의 `300ms` 완만한 펼침·접힘, 개별 근거 박스의 `220ms ease-out` 펼침과, 내용이 화면 밖으로 벗어날 때만 수행하는 최소 거리 시점 이동
- 주요 결과 카드와 상세 섹션의 8px 이하 진입 이동
- 판정 아이콘의 3% 이하 1회 scale
- 팝업 완료 결과 블록의 짧은 stagger
- 로딩 영역의 낮은 대비 pulse
- 복사 완료 상태 전환

일반 UI 전환은 `120ms ~ 200ms`, 개별 근거 Accordion은 최대 `220ms`, 콘텐츠가 큰 L2/L3/L4 레이어 Accordion은 `300ms`, 결과와 섹션 진입은 `180ms ~ 240ms`의 `ease-out`을 사용한다. 바깥 레이어를 닫으면 내부의 펼친 근거도 초기화한다. Loading indicator는 `1s linear infinite`를 사용한다.

### 모션 감소

`prefers-reduced-motion: reduce`에서는 이동·회전 애니메이션을 제거하고 상태를 즉시 표시한다. 상태 안내와 키보드 포커스는 유지한다.

### 제한

- 과도한 page transition
- 상태와 관계없는 반복 animation
- 12px를 넘는 이동 또는 3%를 넘는 scale 변화
- glow와 배경 전체 flash
- 애니메이션이 끝날 때까지 조작이나 핵심 정보 표시를 막는 동작

---

# 45. 부분 분석 실패

고속 분석 중 일부 단계가 실패하더라도 가능한 결과가 존재하면 전체 실패 처리하지 않는다.

예:

```text
L2 성공
L3 일부 실패
```

→ 가능한 결과 반환

사용자에게는 결과 설명 아래 작은 보조 배너로 안내한다.

예:

```text
일부 콘텐츠 분석을 완료하지 못했습니다.
```

핵심 단계가 실패하여 결과 자체를 만들 수 없는 경우에만 전체 분석 실패 처리한다.

---

# 46. 고속 분석 전체 실패

별도 오류 페이지로 이동하지 않는다.

Popup 내부에서 처리한다.

예:

```text
분석을 완료하지 못했습니다.
잠시 후 다시 시도해주세요.

[다시 시도]
```

---

# 47. 결과 유지

고속 분석 결과는 자동 만료시키지 않는다.

다음 중 하나가 발생할 때까지 유지한다.

- 사용자가 `종료` 클릭
- 사용자가 새 분석 시작

---

# 48. 심층 분석 재실행 정책

상세 분석 페이지에는 다음 기능을 제공하지 않는다.

- 다시 분석
- 새 분석
- 결과 재실행

상세 페이지는 해당 `job_id`의:

- 분석 상태
- 완료 결과

를 확인하는 용도로 제한한다.

---

# 49. 공유 / 내보내기

초기 버전에서는 다음 기능을 제공하지 않는다.

- 공유
- PDF Export
- JSON Download
- 리포트 다운로드
- 외부 링크 공유

후순위 기능으로 둔다.

---

# 50. 반응형

상세 분석 페이지는 **Desktop First**로 설계한다.

단, 모바일에서도 깨지지 않는 기본 반응형 구조를 지원한다.

데스크톱:

```text
max-width: 960px
```

모바일 / 태블릿:

- 좌우 여백 축소
- 한 열 구조 유지
- 카드와 아코디언 full-width
- 긴 기술 값 말줄임 또는 줄바꿈
- 버튼 필요 시 full-width

별도의 모바일 전용 UX는 초기 버전에서 만들지 않는다.

---

# 51. 접근성

최소 접근성 기준을 적용한다.

- 상태를 색상만으로 표현하지 않음
- `아이콘 + 텍스트` 함께 사용
- 버튼 및 입력창 focus 상태 제공
- 충분한 명암 대비
- Accordion 키보드 조작 지원
- 복사 버튼 키보드 조작 지원
- Loading / 완료 / 오류 상태를 screen reader가 인식할 수 있도록 처리

---

# 52. 문구 톤

보안 제품이지만 과도한 공포 표현이나 확정적 표현을 사용하지 않는다.

권장:

```text
위험 신호가 확인되었습니다.
악성 가능성이 높은 신호가 확인되었습니다.
의심스러운 동작이 발견되었습니다.
```

지양:

```text
이 사이트는 반드시 악성입니다.
즉시 종료하세요.
당신의 정보가 탈취됩니다.
```

전체적으로:

- 중립적
- 설명 중심
- 과도한 명령형 지양
- 분석 근거 중심

의 문구를 사용한다.

---

# 53. Font

폰트는 시스템 폰트로 임의 결정하지 않는다.

우선순위:

1. `DESIGN.md`에 명시된 Font
2. 해당 Font의 fallback
3. System Font

단, URL / Domain / Hash 등 기술 값에는 monospace 폰트를 별도 적용할 수 있다.

---

# 54. Button Role

Button Style 자체는 `DESIGN.md`를 따른다.

역할 구분:

### Primary

- 분석 시작
- 심층 분석

### Secondary

- 종료
- 분석 취소
- 닫기

### Disabled

- 분석 진행 중
- URL 미입력 / 유효하지 않음

---

# 55. 최종 사용자 플로우

```text
[Chrome Popup Open]
        │
        ▼
[현재 탭 URL 자동 입력]
        │
        ├─ 사용자 URL 수정 가능
        │
        ▼
[분석 시작]
        │
        ▼
[고속 분석]
        │
        └─ 스캔 중...
        │
        ▼
[고속 분석 결과]
        │
        ├─ 안전 / 주의 / 위험
        ├─ 한줄 설명
        └─ 핵심 근거 최대 3개
        │
        ▼
[더 자세한 분석을 진행할까요?]
        │
        ├───────────────┐
        │               │
      [종료]        [심층 분석]
        │               │
        ▼               ▼
[초기 화면]       [/analysis/{job_id}]
                        │
                        ▼
                    [스캔 중...]
                        │
              ┌─────────┴─────────┐
              │                   │
           [취소]               [완료]
              │                   │
              ▼                   ▼
       [취소 상태]         [최종 판정]
                              │
                              ├─ 초기 분석 결과
                              ├─ 핵심 근거 최대 5개
                              ├─ L2
                              ├─ L3
                              └─ L4
```

---

# 56. 구현 문서 관계

프로젝트에서는 다음 3개 문서를 함께 사용한다.

```text
DESIGN.md
  └─ Visual Design System

UI_SPEC.md
  └─ Screen Structure / UX / State Flow

AGENTS.md
  └─ AI Implementation Rules
```

AI Coding Agent는 UI 구현 전 `DESIGN.md`와 `UI_SPEC.md`를 모두 확인해야 한다.

---

# 57. 초기 버전 제외 기능

다음 기능은 초기 버전에서 제외한다.

- 다크 모드
- 결과 공유
- 결과 Export
- 상세 페이지 재분석
- 여러 고속 분석 동시 실행
- 숫자형 위험도 점수 노출
- confidence 노출
- 분석 결과 URL 직접 링크
- 심층 분석 진행률 %
- 심층 분석 세부 내부 단계 노출
- 모든 정상 / 미탐지 항목 나열
